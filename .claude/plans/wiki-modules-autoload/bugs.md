# Bugs — wiki-modules-autoload

## open

- [nit] Dockerfile COPY+checksum layer not exercise-able from worktree `docker build` — worktree lacks `files/`, `patches/`, and other untracked assets required by earlier stages. Build correctness verified by testing against main dev directory. → defer · will be exercised on first full rebuild from main dev at plan completion.

## closed
